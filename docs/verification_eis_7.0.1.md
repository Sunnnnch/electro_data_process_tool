# ElectroChem 7.0.1 EIS 扩展验收

检查日期：2026-09-08。本记录对应最终 `dist_7_0_1_eis_checked` 客户端与 `dist_installer_7_0_1_eis_checked` 安装包。此前的 `dist_7_0_1_eis` 是验收候选，最终包已修正原生验收发现的 R2 标签问题。

## 完成范围

- 六种电路：单时间常数 RC/CPE、半无限 Warburg RC/CPE、双时间常数 RC/CPE。
- Hz 闭区间、均匀/模值权重、确定性多初值、拟合残差、局部近似 95% 参数区间及可辨识性提示。
- 独立 Lin-KK 检查；参数触边、强相关、不可区分支路和无法检查的数据均保留复核原因。
- Nyquist/Bode 拟合叠图、残差图、逐点 CSV、完整诊断 JSON、历史复算和可复现报告。
- 参数模板、内置助手与 MCP 使用同一参数定义；助手可查询模型并读取紧凑诊断摘要。
- 双支路 R2 的标签与电阻单位正确，其他模块的 R² 保持原含义。CPE 指数不同或缺失时，不计算 Q 的差值或合并统计；频段/权重差异进入重复实验的可比性检查。
- 中英文使用说明同步更新；保留现代简洁、纸感编辑、柔和模块、像素复古四种风格。

方法、公式、单位和限制见 [EIS 拟合说明](eis_fitting.md)。当前扩散模型为半无限 Warburg；未包含有限长度扩散或感抗。KK 经验门槛、数值拟合通过和参数物理解释是不同结论。

## 测试证据与例外

完整回归执行 **1,616 项**：1,615 项通过，1 项旧 ZIP 恢复测试在首次发布缓存目录时遇到 Windows `WinError 5`，无跳过或收集错误。随后独立运行该模块 **11/11** 通过，失败用例在 **10 个新目录中 10/10** 通过。路径长度和目录权限检查未发现差异，无法追溯当时的临时句柄，也未改动归档恢复源码。最初选错解释器导致的 MCP 依赖收集错误另行保留，未计为功能测试执行。

原生验收发现旧界面规则把双支路电阻 R2 显示为 R²，已修复；之后 **50 项** EIS、静态界面及旧项目回放测试通过，追加的薄历史记录断言通过。合并去重后，最终 **1,617 个不同测试均有通过记录**。这是完整运行和定向复测的合并结论，不是另一次单独全套全绿运行。

最终 Python 源码与完整覆盖率运行一致，代码行覆盖率 **83.32%**。Ruff、Pyright 和 42 份前端 JavaScript 语法检查通过。打包环境使用自己的 NumPy 2.3.3、SciPy 1.16.2，额外运行 **53 项拟合＋26 项 KK，共 79 项科学测试**，全部通过。

## 最终客户端与分发文件

最终 Windows/WebView2 客户端 **6 项原生流程验收全部通过**：六模型控件、双时间常数 RC＋KK 真实处理、历史参数与诊断、清空频段后的复算、重启恢复、1024 像素宽窗口的手册阅读。97 个有效点中选取 73 点分析，产生 12 个结果文件；复算后这 12 个原文件的 SHA256 保持不变。测试窗口、服务和调试端口均正常关闭，无残留测试进程。

已核验 237 个源文件、61 个静态资源，主程序/MCP 内嵌的 140/136 个应用模块与冻结源码一致。安装包与便携包的版本为 7.0.1，图标的 9 个尺寸与选定图标一致；ZIP 的 2,977 个文件通过 CRC 检查，包内无用户数据。测试使用隔离数据目录，未安装、发布或覆盖原客户端。

| 文件 | SHA256 |
| --- | --- |
| `dist_installer_7_0_1_eis_checked/ElectroChem-Setup-7.0.1.exe` | `ed30fbf341bed47fac351857f6827e297bd7167e1d14742caac05778e9612975` |
| `dist_7_0_1_eis_checked/ElectroChem-7.0.1-win64.zip` | `ed8416e3a0d8daf7c769c69b7f251bb1d2b61fa8bee608160018cd34a13a34a6` |

本地详细证据保存在 `.test_runtime/eis-fullsuite/final-verification.json`、`.test_runtime/eis-native/report.json`、`.test_runtime/eis-checked-build/distribution-manifest.json` 和 `.test_runtime/eis-checked-build/final-review.json`。计算验证采用解析基准、合成数据与文本夹具，不等同于具体仪器或实验体系的校准。
