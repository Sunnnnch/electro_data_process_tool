# 外观与阅读体验改造

改造前已在本地 Git 保存完整检查点 `cb80fbd`，提交说明为 `chore: save verified project and assistant workflow before theme redesign`。

## 完成范围

| 项目 | 实现与验证要点 |
| --- | --- |
| 四套主题 | 首次访问采用实验室浅色；保留深海、暗色、像素主题，旧选择迁移。背景网格默认关闭，减轻像素边框和阴影。 |
| 可读性 | 工作台、助手、设置侧栏采用语义颜色；检查实际渲染背景上的正文、提示、按钮、输入框和任务状态。 |
| 操作层级 | 主要、次要、删除、禁用、悬停、焦点有统一样式；项目右上三个操作按钮在四主题及两种密度下保持等高。 |
| 助手阅读 | 正文 15/17/19px，舒适/紧凑行距；长回答使用可用宽度，代码和宽表格可以键盘滚动，保留内容和操作卡。 |
| 图表 | 矢量预览提供跟随主题和论文白底；数据系列的颜色、形状、坐标保持稳定。PNG 原图白纸展示，不调色；SVG 导出仍为白底。 |
| 独立偏好 | 字号、密度、网格、图表背景与主题分开保存；主题切换不改变项目、结果选择、输入文件与计算载荷。 |
| 设置入口 | 顶部“外观”打开预览卡片弹窗，支持跟随系统、恢复默认、中英文、Escape 关闭和返回入口焦点；保存失败显示本次页面生效提示。 |

## 验证

使用隔离数据目录和本地测试服务；没有操作运行中的用户服务或用户数据。

- 真实浏览器覆盖首次绘制、四个旧主题迁移、损坏设置、系统主题变化、手动覆盖、保存/刷新/恢复默认、保存失败、键盘与语言切换。
- 工作台使用实际处理产生的记录与任务状态，检查四主题正文对比度至少 4.5:1、输入框边界至少 3:1、按钮等高，以及 600px 宽屏幕、18px 字号和两种密度下的布局。
- 助手检查长回答、代码、24 列表格、操作确认和过期/禁用状态；局部滚动不会挤出页面。
- 图表检查 SVG 节点、数据坐标和系列身份不变，轴线/轮廓对比度至少 3:1、文字至少 4.5:1；同一数据的 CSV/SVG 下载字节在主题切换前后相同，PNG 哈希不变。
- Ruff、Pyright、修改的 JavaScript 语法及启动 `python run_v6.py check` 通过。

人工检查了浅色项目页、暗色专业模式、像素外观弹窗，以及助手代码/宽表、暗色 SVG 和项目 PNG 白纸预览。截图保存在本地 `.test_runtime/appearance-workbench`、`.test_runtime/appearance-assistant` 和 `.test_runtime/appearance-charts`；这些测试产物不纳入源码提交。

最终全量回归 **1182 项全部通过**（229.81 秒），启用 `ELECTROCHEM_REQUIRE_PLAYWRIGHT=1`，无跳过。日志位于本地 `.test_runtime/appearance-final-suite.log`。全库 Ruff 通过，Pyright 为 0 errors / 0 warnings，8 个修改/新增前端脚本的语法检查通过，Git 差异空白检查通过。

## 使用说明

见 [外观设置](../docs/appearance.md)。外观偏好仅保存在当前浏览器；旧版本的已保存主题优先保留。图表预览选项仅影响支持适配的矢量图，已经生成的 PNG 使用原图白底。计算方法与原始数据不受外观设置影响。
