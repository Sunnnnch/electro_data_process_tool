# Changelog

## 6.0.1

- 发布独立可运行的 `v6` 仓库版本
- 支持 `python run_v6.py --port 8010` 直接启动服务
- 提供 `setup.bat` 与 `start.bat`
- 内化 `store` / `llm` / `agent` / `processing_core` 运行依赖
- 通过最小运行验证：
  - `check`
  - `smoke`
  - `server` 相关测试
  - `e2e real data`
  - `stress`
